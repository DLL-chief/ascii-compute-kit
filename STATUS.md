# STATUS

Обновлено: 2026-09-14.

## Сейчас

Этап «документация и контракты». Репозиторий `DLL-chief/ascii-compute-kit` создан, публичный. Кода приложения нет.

Сделано:

* Выбрана тема первого фильтра: ASCII-art (рамп по яркости; цифровая мозаика — следующий фильтр в том же пайплайне, не сейчас).
* Зафиксированы стек, отказ от бэкенда, отказ от SAB, публикация через GitHub Pages.
* Описаны архитектура, контракты бэкендов, ADR-001.

## Блокеры

Нет. Ждём явной команды владельца писать код по этой документации.

## Следующий шаг (не начинать без команды)

Реализовать минимальный каркас:

1. `index.html` + панель детекта возможностей.
2. Генерация тестовой картинки 512×512 (без обязательной загрузки файла).
3. ASCII-фильтр на JS в главном потоке.
4. Тот же фильтр в Worker.
5. Заготовка Wasm-бэкенда и GPU-бэкенда по контракту (можно заглушка с тем же результатом, затем наполнение).
6. `.nojekyll` и включение GitHub Pages.

## Беклог (не текущий спринт)

* Фильтр «только цифры 0–9».
* Цветной ASCII (глиф + средний цвет ячейки).
* Загрузка своего изображения.
* Сборка `.wasm` из C/Rust в CI.
* WebGL2 fallback, если WebGPU нет.
* Замер времени по бэкендам в UI.
