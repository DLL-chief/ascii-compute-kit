# Claude Code / любой агент в IDE

Этот файл не дублирует продуктовую документацию. Он задаёт окружение.

Сначала прочитай `AGENTS.md`. Потом `STATUS.md`.

## Репозиторий

* GitHub: https://github.com/DLL-chief/ascii-compute-kit
* Ветка по умолчанию: `main`
* Владелец: DLL-chief
* Лицензия: пока не выбрана; не добавляй LICENSE без вопроса владельцу.

## Локальный запуск (когда появится код)

Статический сервер из корня репозитория, например `python3 -m http.server 8080`. Сборки нет.

## Ограничения среды

* Нет backend, нет `.env`, нет npm-скриптов.
* Проверка — глазами в Chrome (желательно и Android Chrome) и запасной браузер Firefox.
* GitHub Pages не даёт кастомные заголовки COOP/COEP.

## Что трогать не нужно без запроса

* Не переименовывай репозиторий и не переноси документацию в Wiki.
* Не включай Jekyll-темы.
* Не добавляй GitLab CI «для симметрии с шаблоном»: CI, если появится, будет GitHub Actions.
