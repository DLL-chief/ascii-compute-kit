# Claude Code

Читай сначала `AGENTS.md`, затем `STATUS.md`. Этот файл не дублирует правила — только вход.

Репозиторий: `DLL-chief/ascii-compute-kit`. Документация и код на русском. Контракт бэкендов — `docs/api_contracts.md`.
