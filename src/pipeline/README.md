# src/pipeline

Ответственность: принять кадр и команду UI, выбрать backend, вызвать `init` при первом использовании, замерить `run`, при ошибке откатиться по цепочке деградации.

Не делает: не считает ASCII сам, не рисует в canvas.
