# ui

Снимок React-студии из превью. Зависит от React / lucide / radix. Ядро отсюда не импортировать обратно.

Первый кадр: `toAsciiText` в `<pre>`, canvas дорисовывается на клиенте. Кнопки не `disabled` из-за детекта.
